# 4398游戏管理系统开发指南

## 项目概述

本项目是基于 Spring Boot + Vue 3 + TypeScript 的游戏管理系统，包含完整的前后端分离架构。

### 技术栈

后端：Java 21 + Spring Boot 3.2.0 + Spring Data JPA + MySQL 8.0+ + JWT 认证
前端：Vue 3 + TypeScript + Vite + HTML5 + CSS3

---

## 快速开始

### 后端启动

1. 配置数据库环境变量（在 PowerShell 中执行）

```powershell
$env:DB_USERNAME = Read-Host 'MySQL 用户名'
$env:DB_PASSWORD = [System.Net.NetworkCredential]::new('', (Read-Host 'MySQL 密码' -AsSecureString)).Password
$env:JWT_SECRET = [System.Net.NetworkCredential]::new('', (Read-Host 'JWT 随机密钥（至少 32 字节）' -AsSecureString)).Password
```

JWT 密钥应独立随机生成，不能使用示例字符串或重复使用数据库密码。

2. 初始化数据库

```bash
# 进入 MySQL
mysql -u root -p

# 执行初始化脚本
source backend/init.sql
```

或者直接创建数据库：

```sql
CREATE DATABASE game_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

3. 启动后端服务

```bash
cd backend
mvn clean install
mvn spring-boot:run
```

后端地址：http://localhost:8080/api

### 前端启动

1. 安装依赖

```bash
cd frontend
npm install
```

2. 启动 Vite 开发服务器

```bash
npm run dev
```

前端地址：http://localhost:3000

其他命令：

```bash
npm run build       # 构建生产版本
npm run preview     # 预览生产版本
npm run type-check  # TypeScript 类型检查
```

---

## 项目结构

```
4398/
├── backend/                          # 后端项目
│   ├── src/main/java/com/game/system/
│   │   ├── GameManagementSystemApplication.java  # 主启动类
│   │   ├── config/
│   │   │   └── CorsConfig.java                   # 跨域配置
│   │   ├── util/
│   │   │   ├── CommonMethod.java                 # 通用工具类
│   │   │   ├── DateTimeTool.java                 # 日期工具类
│   │   │   └── JwtUtil.java                      # JWT工具类
│   │   ├── payload/
│   │   │   ├── request/DataRequest.java          # 请求封装
│   │   │   └── response/DataResponse.java        # 响应封装
│   │   └── controller/
│   │       └── BaseController.java               # 基础控制器
│   ├── src/main/resources/
│   │   └── application.properties                # 应用配置
│   ├── init.sql                                  # 数据库初始化脚本
│   └── pom.xml                                   # Maven配置
│
└── frontend/                         # 前端项目
    ├── src/
    │   ├── components/               # Vue组件
    │   │   ├── Login.vue             # 登录组件
    │   │   └── Home.vue              # 主页组件
    │   ├── api/
    │   │   └── index.ts              # API接口封装
    │   ├── utils/
    │   │   ├── http.ts               # HTTP请求工具
    │   │   ├── datetime.ts           # 日期工具
    │   │   └── common.ts             # 通用工具
    │   ├── types/
    │   │   └── index.ts              # TypeScript类型定义
    │   ├── main.ts                   # 登录页入口
    │   └── home.ts                   # 主页入口
    ├── assets/
    │   └── style.css                 # 全局样式
    ├── views/
    │   └── home.html                 # 主页HTML
    ├── index.html                    # 登录页HTML
    ├── vite.config.ts                # Vite配置
    ├── tsconfig.json                 # TypeScript配置
    └── package.json                  # NPM配置
```

---

## 核心功能模块

### 后端工具类

#### CommonMethod.java
- 统一响应封装（成功/失败）
- Map数据提取（字符串、整数、日期等）
- 编号生成（2位、3位、4位）
- 数值处理（保留小数）
- JSON转换

#### DateTimeTool.java
- 日期格式化与解析
- 日期计算（增减天/月/年）
- 日期范围（一天的开始/结束）
- 日期差值计算
- 星期几判断

#### JwtUtil.java
- Token生成与验证
- Token刷新
- 从Token提取用户信息

### 前端工具类

#### http.ts
- 封装 Fetch API
- 统一请求处理
- Token认证支持
- 文件上传功能

#### datetime.ts
- 日期格式化
- 日期计算
- 日期验证
- 与后端DateTimeTool对应

#### common.ts
- UUID生成
- 深拷贝
- 防抖/节流
- 表单验证（手机号、邮箱、身份证）
- 本地存储（localStorage/Cookie）
- 文件处理

#### api/index.ts
- 用户API（登录、登出、用户管理）
- 菜单API
- 字典API
- 文件API
- 角色API

---

## Vite + Vue 3 配置说明

### 核心特性

1. 热模块替换（HMR）
   - 代码修改立即生效，无需刷新浏览器
   - Vue组件状态保持

2. API自动代理
   - 前端 `/api/*` 自动代理到 `http://localhost:8080/api`
   - 解决跨域问题

3. TypeScript支持
   - 完整的类型检查
   - Vue 3 组合式API类型提示
   - 路径别名 `@/` = `src/`

4. 极速构建
   - 启动时间 < 1秒
   - 按需编译

### Vue 3 组合式API示例

```vue
<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'

// 响应式数据
const count = ref(0)
const user = reactive({ name: 'admin' })

// 生命周期
onMounted(() => {
  console.log('组件已挂载')
})
</script>
```

### 路径别名使用

```typescript
// 使用别名
import { userApi } from '@/api'
import { storage } from '@/utils/common'

// 不用相对路径
// import { userApi } from '../../api'
```

---

## API接口规范

### 请求格式

```typescript
{
  "data": {
    "key1": "value1",
    "key2": "value2"
  }
}
```

### 响应格式

```typescript
{
  "code": 0,           // 0-成功 1-失败
  "data": {},          // 返回数据
  "msg": "操作成功"     // 返回信息
}
```

---

## 数据库初始化

数据库包含以下表结构：

1. user - 用户表
2. role - 角色表
3. menu - 菜单表
4. role_menu - 角色菜单关联表
5. dictionary_type - 字典类型表
6. dictionary_data - 字典数据表
7. operation_log - 操作日志表

初始化脚本已创建：
- 管理员角色（ROLE_ADMIN）和普通用户角色（ROLE_USER）
- 基础菜单（系统管理、用户管理、角色管理、菜单管理、字典管理）
- 字典示例（用户状态、性别）

注意：初始化脚本不创建默认管理员账号。当前后端尚未完成登录接口及真实用户加载流程，因此登录页面暂不能完成真实登录。完成并验证这些功能后，再通过安全的部署流程创建管理员。

---

## 开发指南

### 添加新接口

1. 后端：创建Controller → Service → Repository
2. 前端：在 `api/index.ts` 添加接口方法
3. 类型定义：在 `types/index.ts` 添加TypeScript接口

### 常用工具方法

后端：

```java
// 返回成功
return CommonMethod.getReturnData(data, "操作成功");

// 返回失败
return CommonMethod.getReturnMessageError("操作失败");

// 格式化日期
String dateStr = DateTimeTool.formatDateTime(new Date(), "yyyy-MM-dd");
```

前端：

```typescript
// 发送请求
const response = await userApi.login({ username, password });

// 格式化日期
const dateStr = formatDate(new Date(), 'yyyy-MM-dd HH:mm:ss');

// 本地存储
storage.set('token', token);
const token = storage.get<string>('token');
```

---

## 开发任务清单

### 已完成

后端框架：
- Spring Boot项目结构
- Maven依赖配置
- 应用配置文件
- 跨域配置
- 数据库初始化脚本
- 所有工具类（CommonMethod、DateTimeTool、JwtUtil等）
- 基础控制器

前端框架：
- Vite + Vue 3 配置
- TypeScript配置
- 所有工具类（http、datetime、common）
- API接口封装
- 登录/主页组件
- 全局样式

### 待完成（按优先级排序）

第一阶段 - 用户认证模块（高优先级）：
1. 实现User实体类
2. 实现UserRepository
3. 完善UserDetailsServiceImpl
4. 实现用户登录接口（AuthController）
   - 登录验证（用户名密码）
   - 生成JWT Token
   - 登录失败统计和账户锁定
5. 实现用户注册接口
6. 验证码接口（图形验证码）
7. 忘记密码功能

第二阶段 - 基础数据管理：
1. 用户管理模块（CRUD）
2. 角色管理模块（CRUD）
3. 菜单管理模块（树形结构）
4. 字典管理模块（CRUD）

第三阶段 - 权限控制：
1. 角色权限关联
2. 用户角色关联
3. 菜单权限控制
4. 按钮级权限控制

第四阶段 - 前端界面：
1. 主框架布局（侧边栏、顶栏、内容区）
2. 用户管理界面
3. 角色管理界面
4. 菜单管理界面

第五阶段 - 增强功能：
1. 文件上传下载
2. 操作日志记录
3. 数据导出（Excel）
4. 系统监控

---

## 开发规范

### 后端开发规范
1. 所有实体类使用JPA注解
2. 使用Lombok简化代码（@Data, @Entity等）
3. Service层使用@Transactional管理事务
4. Controller统一返回DataResponse
5. 所有日期字段使用LocalDateTime
6. 注释必须使用中文

### 前端开发规范
1. 所有文件使用TypeScript
2. 严格遵循类型定义
3. API调用统一使用封装的方法
4. 错误处理要友好提示用户
5. 敏感操作需二次确认
6. 注释必须使用中文

### 数据库规范
1. 表名使用小写+下划线
2. 主键统一使用id
3. 必须包含create_time和update_time
4. 软删除使用deleted字段
5. 外键建立索引

---

## 常见问题

### 后端启动失败

问题：端口被占用
```
Port 8080 was already in use
```

解决：修改端口

```properties
# application.properties
server.port=8081
```

问题：数据库连接失败
```
Communications link failure
```

解决：
- 检查MySQL是否启动
- 检查用户名密码是否正确
- 检查数据库是否存在

### 前端编译失败

问题：TypeScript编译错误

解决：
```bash
# 清理并重新安装依赖
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Vite配置警告

启动时提示：
```
Your Vite config uses features that are unsupported by `configLoader: 'native'`
```

解决：在 `package.json` 添加：
```json
{
  "type": "module"
}
```

或者忽略（不影响开发）。

---

## 注意事项

1. 编码统一：所有文件使用UTF-8编码
2. 时区处理：后端和前端统一使用 `Asia/Shanghai` 时区
3. 安全性：数据库凭据和JWT密钥必须通过环境变量配置；生产环境需审查CORS配置。初始化脚本不创建默认管理员，完整认证流程仍待实现和验证，不应直接部署到公网
4. 数据库：首次运行会自动创建表结构（JPA DDL auto）

---

## 性能优化建议

1. 数据库查询优化（索引、分页）
2. 接口响应缓存（Redis）
3. 前端资源压缩
4. 图片懒加载
5. 静态资源CDN

---
