# 数据库配置说明与修改记录

## 📋 修改概览

本文档记录了对比参考项目（StuProject）后，对当前项目数据库相关配置的调整。

---

## ✅ 已完成的修改

### 1. HikariCP 连接池配置优化

**文件**：`backend/src/main/resources/application.properties`

**修改内容**：
```properties
# 修改前
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20

# 修改后（开发环境优化）
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=10
```

**修改原因**：
- 参考项目使用较小的连接池配置（minimum-idle=1, maximum-pool-size=2）
- 当前项目原配置对开发环境过于激进，可能造成资源浪费
- 调整为中等配置，既保证性能又节省资源

---

### 2. 数据库表结构补充

**文件**：`backend/init.sql`

**新增表结构**：

#### 2.1 游戏表（game）

```sql
CREATE TABLE IF NOT EXISTS `game` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '游戏ID',
    `name` VARCHAR(100) NOT NULL COMMENT '游戏名称',
    `description` TEXT COMMENT '游戏描述',
    `cover_image` VARCHAR(255) COMMENT '封面图片URL',
    `category` VARCHAR(50) COMMENT '游戏分类',
    `tags` VARCHAR(255) COMMENT '游戏标签（逗号分隔）',
    `game_url` VARCHAR(500) COMMENT '游戏链接或路径',
    `status` TINYINT DEFAULT 1 COMMENT '状态：0-下架 1-上架',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_name` (`name`),
    INDEX `idx_status` (`status`),
    INDEX `idx_category` (`category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='游戏表';
```

**字段说明**：
- `id`：游戏唯一标识
- `name`：游戏名称（必填）
- `description`：游戏详细描述
- `cover_image`：游戏封面图片 URL
- `category`：游戏分类（动作、冒险、角色扮演等）
- `tags`：游戏标签，逗号分隔（如："多人,在线,射击"）
- `game_url`：游戏访问链接或本地路径
- `status`：上架状态（0-下架，1-上架）

#### 2.2 用户游戏清单表（user_game_list）

```sql
CREATE TABLE IF NOT EXISTS `user_game_list` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT 'ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `game_id` BIGINT NOT NULL COMMENT '游戏ID',
    `status` TINYINT DEFAULT 1 COMMENT '状态：1-收藏 2-正在玩 3-已完成',
    `play_time` INT DEFAULT 0 COMMENT '游戏时长（分钟）',
    `last_play_time` DATETIME COMMENT '最后游玩时间',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '添加时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    UNIQUE KEY `uk_user_game` (`user_id`, `game_id`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_game_id` (`game_id`),
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`game_id`) REFERENCES `game`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户游戏清单表';
```

**字段说明**：
- `user_id`：用户ID（外键）
- `game_id`：游戏ID（外键）
- `status`：游戏状态（1-收藏，2-正在玩，3-已完成）
- `play_time`：累计游戏时长（分钟）
- `last_play_time`：最后一次游玩时间
- **唯一约束**：一个用户对同一游戏只能有一条记录
- **级联删除**：用户或游戏删除时，自动清理对应的清单记录

#### 2.3 新增字典数据

**游戏相关字典类型**：
- `game_category`：游戏分类（动作、冒险、角色扮演、策略、休闲）
- `game_status`：游戏状态（上架、下架）
- `user_game_status`：用户游戏状态（收藏、正在玩、已完成）

---

## 📊 对比参考项目的差异

### 相同点
✅ 都使用 Spring Boot + Spring Data JPA  
✅ 都使用 MySQL 8.0+  
✅ 都使用 BCrypt 密码加密  
✅ 都使用 JWT 认证  
✅ 都使用 HikariCP 连接池  

### 优化点
✅ **主键类型**：当前项目使用 BIGINT（更好），参考项目使用 Integer  
✅ **时区配置**：当前项目明确设置 Asia/Shanghai  
✅ **连接池配置**：参考项目配置更新后的当前项目连接池更合理  
✅ **索引设计**：当前项目索引设计更完善  

---

## 🔍 参考项目的数据库交互方式

### Repository 层（JPA 接口）

参考项目使用标准的 Spring Data JPA Repository：

```java
@Repository
public interface UserRepository extends JpaRepository<User, Integer> {
    // 方法命名规范查询
    Optional<User> findByUserName(String userName);
    Boolean existsByUserName(String userName);
    
    // JPQL 查询
    @Query(value = "select max(id) from User")
    Integer getMaxId();
    
    // 原生 SQL 查询
    @Query(value = "select * from user where username = ?1", nativeQuery = true)
    Optional<User> findByUserNameNative(String userName);
}
```

**三种查询方式**：
1. **命名规范查询**：`findByXxx`、`existsByXxx`（最推荐）
2. **JPQL 查询**：使用 `@Query` 注解，操作实体类
3. **原生 SQL 查询**：使用 `@Query(nativeQuery = true)`，直接写 SQL

### 实体类设计

参考项目实体类使用：
- `@Entity`：标记为 JPA 实体
- `@Table(name = "user")`：指定表名
- `@Id` + `@GeneratedValue`：主键自增
- `@ManyToOne`、`@OneToOne`、`@ManyToMany`：关联关系
- `Lombok` 注解：`@Getter`、`@Setter` 简化代码

---

## 🎯 当前项目需要创建的文件

基于参考项目的经验，当前项目需要创建：

### 实体类（backend/src/main/java/com/game/system/entity/）
- [ ] `User.java` - 用户实体
- [ ] `Role.java` - 角色实体
- [ ] `Menu.java` - 菜单实体
- [ ] `Game.java` - 游戏实体
- [ ] `UserGameList.java` - 用户游戏清单实体

### Repository 接口（backend/src/main/java/com/game/system/repository/）
- [ ] `UserRepository.java`
- [ ] `RoleRepository.java`
- [ ] `GameRepository.java`
- [ ] `UserGameListRepository.java`

---

## 📝 注意事项

### 开发环境 vs 生产环境

1. **DDL 自动更新策略**
   - 开发环境：`spring.jpa.hibernate.ddl-auto=update`（当前配置）
   - 生产环境：应改为 `validate` 或 `none`

2. **连接池配置**
   - 开发环境：`minimum-idle=2, maximum-pool-size=10`（当前配置）
   - 生产环境：需根据实际负载调整（建议 minimum-idle=10, maximum-pool-size=50）

3. **SQL 日志**
   - 开发环境：`spring.jpa.show-sql=true`（当前配置）
   - 生产环境：应设置为 `false`，避免性能损耗

### 数据安全

✅ **环境变量配置**：已正确使用 `${DB_USERNAME}`、`${DB_PASSWORD}`、`${JWT_SECRET}`  
✅ **密码加密**：已使用 BCrypt  
✅ **无默认管理员**：初始化脚本不创建默认管理员账号（安全）  

---

## ✅ 总结

当前项目的数据库配置已经参考 StuProject 进行了优化：

1. ✅ 连接池配置调整为更合理的开发环境配置
2. ✅ 补充了游戏表和用户游戏清单表
3. ✅ 添加了游戏相关的字典数据
4. ✅ 数据库设计符合规范，索引完善
5. ✅ 外键约束保证数据完整性

**下一步工作**：
- 创建实体类（Entity）
- 创建数据访问层（Repository）
- 实现用户注册和登录接口
