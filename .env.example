# 环境变量配置示例

# 数据库配置
DB_USERNAME=your_mysql_username
DB_PASSWORD=your_mysql_password

# JWT 密钥（至少 32 字节，建议使用随机字符串）
JWT_SECRET=your_jwt_secret_key_at_least_32_bytes_long

# 示例说明：
# 1. 复制此文件并重命名为 .env
# 2. 修改上面的值为实际配置
# 3. 不要将 .env 文件提交到 Git（已在 .gitignore 中）

# 如何设置环境变量：

## Windows PowerShell
# $env:DB_USERNAME = "root"
# $env:DB_PASSWORD = "your_password"
# $env:JWT_SECRET = "your_jwt_secret_key_at_least_32_bytes_long"

## Windows CMD
# set DB_USERNAME=root
# set DB_PASSWORD=your_password
# set JWT_SECRET=your_jwt_secret_key_at_least_32_bytes_long

## Linux / macOS
# export DB_USERNAME=root
# export DB_PASSWORD=your_password
# export JWT_SECRET=your_jwt_secret_key_at_least_32_bytes_long

# 生成安全的 JWT 密钥（建议使用以下方式之一）：

## 方法 1: 使用 PowerShell（Windows）
# -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | ForEach-Object {[char]$_})

## 方法 2: 使用 OpenSSL（Linux/macOS）
# openssl rand -base64 32

## 方法 3: 在线生成器
# https://www.grc.com/passwords.htm
